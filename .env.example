export ZAI_API_KEY=

export ECOM_LLM_TOKEN=
export ECOM_LLM_EXP_TOKEN=

export JIRA_BASE_URL="https://project.samokat.ru"
export JIRA_TOKEN=

export GITLAB_URL="https://git.ecom.tech"
export GITLAB_TOKEN=

export CONFLUENCE_BASE_URL="https://space.samokat.ru"
export CONFLUENCE_TOKEN=

export MCP_ADR_TOKEN=

export ALLURE_BASE_URL="https://allure-testops-spb.samokat.ru"
export ALLURE_TOKEN=

# oc-workspace() {
#   local workspace_name="${1:-}"
#
#   [[ -n "$workspace_name" ]] || {
#     echo "Использование: oc-workspace <workspace> [аргументы opencode]" >&2
#     return 2
#   }
#
#   local workspace="$HOME/workspaces/$workspace_name"
#
#   git -C "$workspace" rev-parse --is-inside-work-tree >/dev/null 2>&1 || {
#     echo "Workspace не найден или это не Git-репозиторий: $workspace" >&2
#     return 1
#   }
#
#   [[ -d "$workspace/.opencode" ]] || {
#     echo "Не найдена .opencode: $workspace/.opencode" >&2
#     return 1
#   }
#
#   git -C "$workspace" pull --ff-only || {
#     echo "Не удалось обновить конфигурацию workspace: $workspace" >&2
#     return 1
#   }
#
#   shift
#   OPENCODE_CONFIG_DIR="$workspace/.opencode" command opencode "$@"
# }
#
# oc-qa() { oc-workspace qa "$@"; }
# oc-sa() { oc-workspace sa "$@"; }
# oc-go() { oc-workspace letsgo "$@"; }