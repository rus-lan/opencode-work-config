{
  "name": "herdr-status",
  "description": "Показывает и обновляет метрики сессии агента в Herdr UI",
  "type": "command",
  "handler": {
    "type": "script",
    "path": "./herdr-status.sh"
  },
  "options": {
    "verbose": {
      "type": "boolean",
      "description": "Show detailed metrics including tokens and cache",
      "default": false
    },
    "watch": {
      "type": "boolean",
      "description": "Continuous monitoring mode",
      "default": false
    },
    "interval": {
      "type": "number",
      "description": "Update interval in seconds (for watch mode)",
      "default": 30
    }
  },
  "usage": [
    "/herdr-status - Show and update agent session metrics in Herdr",
    "/herdr-status --verbose - Show detailed metrics",
    "/herdr-status --watch - Continuous monitoring",
    "/herdr-status --watch --interval 15 - Update every 15 seconds"
  ]
}
