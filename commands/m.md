---
name: m
description: Show session metrics — call get_metrics tool
---

Call the `get_metrics` tool. It returns a formatted dashboard with current session metrics (duration, tokens, cost, model, subagents). Display the result.